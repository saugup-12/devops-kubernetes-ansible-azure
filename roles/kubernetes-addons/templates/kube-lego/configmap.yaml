apiVersion: v1
metadata:
  name: kube-lego
  namespace: kube-lego
data:
  # modify this to specify your address
  lego.email: "{{kube_lego_email}}"
  # configure letencrypt's production api
  lego.url: "{{kube_lego_api_url}}"
kind: ConfigMap
